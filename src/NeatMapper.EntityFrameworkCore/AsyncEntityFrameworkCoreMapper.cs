using System;

namespace NeatMapper.EntityFrameworkCore {
	/// <summary>
	/// <see cref="IAsyncMapper"/> which converts keys to entities, asynchronously, even composite keys as <see cref="Tuple"/> or <see cref="ValueTuple"/>.<br/>
	/// Also supports collections (not nested)
	/// </summary>
	public sealed class AsyncEntityFrameworkCoreMapper {
	}
}
